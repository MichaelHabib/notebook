= Bash Find Command Guide

== Code Samples

.Find directores , format output by using `-exec` & append to file using `>> FileName`
----
find ${pwd} -maxdepth 1 -type d -exec echo "\"text_prefix + %f \"\n" >> _fileList.txt {} \;
----
